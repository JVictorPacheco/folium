DATABASE_URL=postgresql+asyncpg://folium:folium@localhost:5432/folium
JWT_SECRET_KEY=change-me
ACCESS_TOKEN_EXPIRE_MINUTES=10080
STORAGE_DIR=./storage
FRONTEND_URL=http://localhost:5173

# E-mail transacional (SMTP). Vazio = link cai no log (dev).
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=Folium <no-reply@folium.app>
SMTP_USE_TLS=true
